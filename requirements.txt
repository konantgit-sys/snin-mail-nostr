# SNIN Mail — Python dependencies
# Установка: pip3 install -r requirements.txt
# Запуск: uvicorn app:app (ядро mailbridge импортируется из корня)
fastapi>=0.110.0
uvicorn>=0.29.0
websocket-client>=1.8.0
requests>=2.31.0
cryptography>=42.0.0
secp256k1==0.14.0
pycryptodome==3.23.0
httpx2>=0.28.0
